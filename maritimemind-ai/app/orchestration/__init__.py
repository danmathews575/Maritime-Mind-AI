"""MaritimeMind AI — LangGraph Orchestration (Phase 7)"""
# NOTE: Intentionally minimal — importing graph.py at package level triggers
# heavy model loading (embedding, BM25, CLIP). Keep imports lazy.
